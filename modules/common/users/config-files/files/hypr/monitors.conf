# ===================================
# Monitors
# ===================================

# Shared fallback: allow monitors to auto-configure without assuming a specific device name.
monitor=,preferred,auto,1
